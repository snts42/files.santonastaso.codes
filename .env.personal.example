# Personal Development Configuration
# This file contains your personal AWS configuration for ongoing development.
# It is gitignored and will not be committed to the repository.

# AWS Configuration (Your Personal Settings)
AWS_REGION=eu-west-2
AWS_ACCESS_KEY_ID=your_actual_access_key
AWS_SECRET_ACCESS_KEY=your_actual_secret_key
S3_BUCKET_NAME=files-santonastaso-production
DDB_TABLE_NAME=files-santonastaso-metadata

# Application Settings (Your Personal Domain)
ENVIRONMENT=production
LOG_LEVEL=INFO
CORS_ORIGINS=https://files.santonastaso.codes,http://localhost:8000
FRONTEND_BASE_URL=https://files.santonastaso.codes

# File Storage Configuration
MAX_FILE_SIZE_MB=100
DEFAULT_EXPIRY_HOURS=24
PRESIGNED_UPLOAD_TTL_SECONDS=300
PRESIGNED_DOWNLOAD_TTL_SECONDS=300

# Security Settings
ALLOWED_CONTENT_TYPES=image/,text/,application/pdf,application/zip,video/,audio/
RATE_LIMIT_PER_HOUR=50

# Development Settings (LocalStack support maintained)
USE_LOCALSTACK=false
LOCALSTACK_ENDPOINT=http://localhost:4566
LOCALSTACK_AUTOCREATE=false

# Note: Copy this to backend/.env.personal for your development work
# The .env.personal file is gitignored and won't be shared publicly
